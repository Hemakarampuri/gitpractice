apiVersion: v1
Kind: Pod
metadata:
  name: heema
  labels:
    app: nginx
    environment: dev
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
      - containerPort: 8080
        protocol: TCP