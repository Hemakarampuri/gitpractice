apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-
  labels:
    app: nginx
    purpose: svcdemo
    replicas: 3
spec:
  minReadySeconds: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        -  name: nginx
           image: nginx:1.25
           ports:
             - containerPort: 80
               protocol: TCP